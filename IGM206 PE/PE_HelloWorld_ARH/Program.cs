namespace PE_HelloWorld_ARH   // project name for organization; using statements (like import statements), written using *, go above
{ 
    internal class Program  // class
    {
        static void Main(string[] args)  // main line function
        {
             Console.WriteLine("Hello, World!");  // write command
        }
    }

     /* 
     Comments work the same
     F5 = run w/ debugging, shows debugging info below
     */
}
